spring:
  cloud:
    function:
      scan:
        packages: org.debugroom.mynavi.sample.sonarqube.initdb.app.function
cloud:
  aws:
    stack:
      auto: false
    credentials:
      instanceProfile: false
      profileName:
    region:
      auto: false
      static: ap-northeast-1
logging:
  level:
    org.springframework.web.client.RestTemplate: DEBUG